<% if @clap.count < 10 %>
  <%= turbo_stream.update "claps-count-#{@diary.id}", @diary.count_all_claps %>
<% else %>
  <%= turbo_stream.update "claps-count-#{@diary.id}", @diary.count_all_claps %>
  <%= turbo_stream.replace "clap-#{@diary.id}" do %>
    <% render 'diaries/max_claps' %>
  <% end %>
<% end %>